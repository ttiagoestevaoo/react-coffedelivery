import styled from "styled-components";

export const TitleLContainer = styled.h2`
    font-family: 'Baloo 2';
    font-size: 2rem;
    font-weight: 800;
    line-height: 1.3;
    color: ${(props) => props.theme["yellow-dark"]}
`